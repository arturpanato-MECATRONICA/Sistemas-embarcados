const int leda = 9;
const int ledb = 10;
const int ledc = 11;
const int ledd= 12;

const int botaoa = 2; 
const int  botaob = 3;

void setup() {
  pinmode(botaoa, INTPUT_PULLUP);
  pinMode(pinbotaob, INTPUT_PULLUP);
}
  pinMode(leda, OUTPUT);
  pinmMode(ledb, OUTPUT);
  pinMode(ledc, OUTPUT)
  pinMode(ledd,OUTPUT);
  }

  if (leda == 1, ledb == 0);